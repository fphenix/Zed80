PPI for Keyboard read

use Log class in D7 as well

add a dump image function (dump the screen picuture at will)

regen vector func isVector list

move all flag compute to func (in progress...)

check for sub and add the sign (0 minus 1 must be 0xff): 2's complement

H flag of ADD HL, DE seems wrong

do a few tests for each opcode

review flags

review Overflow, borrow, half borrow, carry half carry